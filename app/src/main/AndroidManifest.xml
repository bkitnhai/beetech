<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nguyehai.shoptest5">

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-sdk tools:overrideLibrary="com.paypal.android.sdk.payments"/>
    <application
        android:name=".all.products.backEndOfAllPhones.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Recents" />
        <activity
            android:name=".all.products.All_Products"
            android:label="@string/title_activity_songs" />
        <activity android:name=".carts.Carts" />
        <activity android:name=".all.products.All_Products_iPhone7" />
        <activity android:name=".all.products.All_Products_iPhone7_Detail"></activity>
    </application>

</manifest>